{
  "type": "module",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.4.4",
    "@commitlint/config-conventional": "^20.4.4",
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.3",
    "husky": "^9.1.7",
    "lint-staged": "^16.3.3",
    "prettier": "^3.8.1",
    "vite-svg-loader": "^5.1.1"
  },
  "lint-staged": {
    "practical-test/**/*.{js,ts,vue}": [
      "eslint --fix",
      "prettier --write"
    ],
    "practical-test/**/*.{css,scss}": [
      "prettier --write"
    ]
  }
}
